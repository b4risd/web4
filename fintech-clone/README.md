# Fintech Clone (Cosmos Tasks UI)

- Open `index.html` directly or serve the folder:

```bash
python3 -m http.server 8080 -d /workspace/fintech-clone
```

Then visit http://localhost:8080/ (or your workspace preview URL).

Note: Assets are loaded from the original domain to render identically.